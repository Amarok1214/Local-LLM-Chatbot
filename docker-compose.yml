services:
  falkordb:
    image: falkordb/falkordb:latest
    ports:
      - "6379:6379"    # Redis protocol
      - "3001:3000"    # FalkorDB Browser (mapped to 3001)
    volumes:
      - falkordb_data:/data
    restart: unless-stopped

volumes:
  falkordb_data:
